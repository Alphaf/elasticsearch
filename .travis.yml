language: java
install: true


dist: trusty
jdk:
  - oraclejdk8


matrix:
  allow_failures:
    - jdk: openjdk8

branches:
  only:
  - master
    
    

script:
  - ./gradlew --continue clean build --scan -s -p modules/
